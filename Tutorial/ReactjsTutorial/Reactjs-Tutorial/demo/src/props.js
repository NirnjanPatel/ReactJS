// Props
// ------
// * are arguments passed into React components.
// * are passed to components via HTML attributes.

function prop(props) { 
    return <h2>Car is { props}</h2>
}
export default prop;